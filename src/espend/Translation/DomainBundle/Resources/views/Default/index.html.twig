
{# should registered in compiled file: goto, completion, annotator #}
{{ 'yaml.symfony.great'|trans({}, 'Yaml') }}

{# should registered in compiled file: completion, annotator  #}
{{ 'xlf.symfony.great'|trans({}, 'Interchange') }}

{# should registered in compiled file: completion, annotator  #}
{{ 'gettext.symfony.great'|trans({}, 'GetText') }}


{# --- weak --- #}

{# should valid because of indexer: goto, completion, annotator  #}
{{ 'yaml_weak.symfony.great'|trans({}, 'WeakYaml') }}

{# not supported ! #}
{{ 'xlf_weak.symfony.great'|trans({}, 'WeakInterchange') }}

{# not supported ! #}
{{ 'gettext_weak.symfony.great'|trans({}, 'WeakGetText') }}


{# --- TODO: pattern fix for this --- #}
{{ 'form.tab.profile'|trans|desc('Interchange') }}
{{ 'form.tab.profile'|trans({}, 'AcmeBundle')|desc('Profile') }}

{# --- TODO: pattern fix for this --- #}
{{ sonata_block_render({
    'type': 'nice_block',
    'settings': {
        'title': 'block.nice.title'|trans,
        'more_url': path('more_nice_stuff'),
        'sub_partner_id': app.request.query.get('sub_partner_id')
    }
    })
}}